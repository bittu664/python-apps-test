apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-helm-apps.fullname" . }}-test-connection"
  labels:
{{ include "python-helm-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "python-helm-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
